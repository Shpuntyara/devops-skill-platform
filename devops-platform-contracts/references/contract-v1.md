# Contract v1

Every executor receives: target, environment, objective, confirmed facts, constraints, risk, and selected capabilities. Every executor returns: discovery, decision, exact change, risk, rollback, verification evidence, and handoff.

A module can add constraints but cannot downgrade an approval class or state-recovery requirement from the default policy. An absent module is a bounded handoff condition, not permission to improvise.